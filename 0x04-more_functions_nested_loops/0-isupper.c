#include "main.h"

/**
 * main - Entry point
 *_isupper- check for uppercase character
 * Return: Always 1 (success) otherwise 0
 */

int _isupper(int c)
{

if (c >= 65 && c <= 97)
{
return (1);
}
return (0);
}
